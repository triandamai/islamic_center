class currentUser {
  String uid, displayName, email, photoUrl;
  bool loginStatus;
  currentUser(
      {this.uid,
      this.displayName,
      this.email,
      this.photoUrl,
      this.loginStatus});
}
